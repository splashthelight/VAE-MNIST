[project]
name = "vae-mnist"
version = "0.1.0"
description = "VAE (Variational Autoencoder) training and generation on MNIST"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.1",
    "numpy>=2.5.1",
    "torch>=2.13.0",
    "torchvision>=0.28.0",
    "tqdm>=4.69.1",
]

[[tool.uv.index]]
url = "https://mirrors.cloud.tencent.com/pypi/simple"
default = true
